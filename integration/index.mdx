---
sidebar_position: 0
title: Build with Atto
description: Add feeless micropayments to AI agents, terminal scripts, N8N workflows, and applications. Choose an integration and start with a read-only operation.
hide_table_of_contents: true
---
import { faq } from "./faq";
import Head from "@docusaurus/Head";
import GuideCards from "@site/src/components/Docs/GuideCards";

# Build with Atto {#integration-overview}

Add feeless micropayments to the software you already use. Connect an AI agent, work from a terminal, build an N8N workflow, or bring Atto directly into your application.

## Choose your integration

<GuideCards items={[
  {title: 'MCP', label: 'For AI agents', description: 'Give your assistant wallet tools. Start read-only, then approve spending locally within limits you choose.', href: '/docs/integration/mcp'},
  {title: 'CLI', label: 'For terminals and scripts', description: 'Manage a wallet, inspect the network, and bring payment commands into your own scripts.', href: '/docs/integration/cli'},
  {title: 'N8N', label: 'For workflows', description: 'Read accounts, act on incoming payments, and add payment steps to self-hosted N8N.', href: '/docs/integration/n8n'},
  {title: 'Commons', label: 'For application code', description: 'Choose protocol primitives, node clients, wallet tools, signers, and workers from a modular library.', href: '/docs/integration/commons'},
]} />

## Start with a read

You do not need spending access to explore the network. The guides begin with an address, a balance check, or a local amount conversion. Add wallet credentials and payment permissions only when your application needs them.

If you want to try the data formats before writing code, open the [browser tools](/docs/tools). For a wallet without an integration, start with [Wallet](/wallet).

## Why Atto? {#why-atto}

There is no protocol transaction fee to subtract from a small payment. Amounts support nine decimal places, and independent account chains can progress concurrently. See the [network measurements](/metrics) and [technical whitepaper](/docs/whitepaper/technical) for timing and protocol details.

:::note Two sides of a payment
A confirmed send creates a receivable for its destination. A receive or open block adds that amount to the recipient's account balance. Distinguish a payment arriving from funds becoming spendable when designing your application.
:::

## Common Use Cases {#common-use-cases}

| You are building… | Start here |
| --- | --- |
| An agent that checks balances or makes approved payments | [MCP guide](/docs/integration/mcp) |
| A terminal workflow or payment script | [CLI guide](/docs/integration/cli) |
| Automation triggered by incoming payments | [N8N guide](/docs/integration/n8n) |
| A custom wallet or payment application | [Commons guide](/docs/integration/commons) |
| A backend using wallet REST operations | [Wallet Server](/docs/integration/wallet-server) and [Wallet API](/api/wallet) |
| A service reading ledger data | [Node API](/api/node) and [node deployment](/docs/integration/node) |

## Run your own infrastructure

The integrations above and the deployment references below serve different needs. Use the node, wallet, signer, and work-server guides when you want to operate those services yourself.

### Components {#components}

| Component              | Default Ports      | Role                                                        | Storage |
|------------------------|--------------------|-------------------------------------------------------------|---------|
| **historical node**    | 8080 / 8081 / 8082 | Full ledger (REST enabled)                                  | MySQL   |
| **voter node**         | 8080 / 8081 / 8082 | Maintains the ledger and signs consensus votes              | MySQL   |
| **signer** *(sidecar)* | Configurable       | Hardware‑backed vote signing for the voting node (optional) | No      |
| **work‑server**        | 8080 / 8081        | Computes PoW required to submit transactions                | No      |
| **wallet‑server**      | 8080 / 8081        | REST wallet ops, send and receive                           | MySQL   |

:::tip
Voting and historical nodes use the **same container image**—the presence of the `ATTO_PRIVATE_KEY` env‑var (or an external
signer) toggles the mode. Switching modes later isn't supported; choose once or plan a fresh bootstrap.
:::

Follow the [node guide](/docs/integration/node) for historical and voter roles, the [wallet-server guide](/docs/integration/wallet-server) for wallet REST operations, the [work-server guide](/docs/integration/work-server) for proof-of-work, and the [signer guide](/docs/integration/signer) for external signing.

### Ports Cheat‑Sheet {#ports-cheatsheet}

| Port     | Purpose                                    | Exposure                                                     |
|----------|--------------------------------------------|--------------------------------------------------------------|
| **8080** | REST Interface                             | Cluster‑internal only.                                       |
| **8081** | `/health/liveness`, `/health/readiness` & `/prometheus` | Cluster‑internal only.                                       |
| **8082** | Node‑to‑node gossip (WebSocket)            | Terminate TLS at the load‑balancer / ingress. Public‑facing. |

For lower-level transaction construction, use [offline signing with Commons](/docs/integration/advanced/offline-signing-with-atto-commons) and the [protocol reference](/docs/integration/advanced/protocol-offline-signing-reference). Keep production secrets out of tutorial examples and logs.

## Frequently Asked Questions (FAQ)

<Head>
  <script type="application/ld+json">
    {JSON.stringify({
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": faq.map((faq) => ({
        "@type": "Question",
        name: faq.question,
        acceptedAnswer: {
          "@type": "Answer",
          text: faq.link
            ? `${faq.answer}${faq.link.label}${faq.link.suffix ?? ""}`
            : faq.answer
        }
      }))
    })}
  </script>
</Head>

{faq.map((faq, index) => (
<details key={index} style={{ marginBottom: "1.5rem" }}>
  <summary><strong>{faq.question}</strong></summary>
  <p>
    {faq.answer}
    {faq.link && <a href={faq.link.href}>{faq.link.label}</a>}
    {faq.link?.suffix}
  </p>
</details>
))}
